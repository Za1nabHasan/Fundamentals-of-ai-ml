# CIFAR-10 Image Classification Using CNN

## Overview

This project implements an image classification system using a Convolutional Neural Network (CNN) and the CIFAR-10 dataset. The model is trained to classify images into 10 different categories: airplane, automobile, bird, cat, deer, dog, frog, horse, ship, and truck.

The project demonstrates the basic workflow of a deep learning image-classification system, including data loading, preprocessing, model training, evaluation, and prediction.

## Features

- Loads the CIFAR-10 dataset.
- Preprocesses and normalizes image data.
- Converts class labels into categorical form.
- Builds a CNN-based image classification model.
- Trains the model using training data.
- Evaluates model performance using test data.
- Generates predictions for test images.
- Saves the trained model for future use.
- Displays sample images and classification results.

## Technologies / Tools Used

- **Python**
- **TensorFlow**
- **Keras**
- **NumPy**
- **Matplotlib**
- **PIL (Pillow)**
- **Jupyter Notebook / VS Code**

## Installation & Running

### 1. Clone the repository
Clone the project repository using Git:

```bash
   git clone https://github.com/Za1nabHasan/Fundamentals-of-ai-ml.git


Instructions for Testing

The project can be tested using the CIFAR-10 test dataset.

Run the notebook and verify that the CIFAR-10 training and testing datasets are loaded successfully.
Verify that the images are preprocessed and normalized correctly.
Run model.summary() and confirm that the model architecture is displayed without errors.
Run the training cells and verify that training and validation loss and accuracy are displayed.
Evaluate the trained model on the test dataset and verify that test loss and test accuracy are generated.
Run the prediction section using sample test images and verify that the model produces a predicted class from the ten CIFAR-10 categories.
Verify that the trained model is successfully saved as cifar10_classifier_model.h5.

Successful completion of these steps confirms that the main components of the project—including dataset loading, preprocessing, model training, evaluation, prediction, and model saving—are functioning correctly.
git clone <repository-url>

#Screenshots:
<img width="825" height="680" alt="image" src="https://github.com/user-attachments/assets/364e441f-b07b-4677-be14-a111b777f74c" />
<img width="825" height="433" alt="image" src="https://github.com/user-attachments/assets/8816ed11-2cdc-46d4-8fbe-d30e9a634f65" />

